# RAG 表格处理经验笔记

> 一句话总结：PDF 里的表格，pymupdf 默认按"列优先"提取文字，会打乱行列对应关系，
> 导致 RAG 检索到表格时数字"张冠李戴"。解法：按视觉坐标"行优先"提取 + 表格行"标签前置"。

---

## 一、背景

在给数字员工做 RAG 评估时，用 30 个问题（问题+标准答案）跑了阶段2（混合检索+重排）。
发现一个反直觉现象：阶段2 在"方法细节类"题目上大幅领先，但在"实验数据类"（问具体分数）反而翻车——
问"消融中 DTQL 在 Hopper 的分数"（答案 99.6），它答成了 70.7；问"VBD-AA 的分数"（答案 98.6），答成 96.3。

这些全是"表格题"，错法高度一致：数字答的是"相邻行列"的值，即"张冠李戴"。

## 二、根因分析（三层，逐层深入）

### 第 1 层：pymupdf 默认"列优先"提取，把表格拆散了

`page.get_text()` 对表格是"按列竖着抽"的。论文的消融表（Table 2）本来长这样：

    Method   HC-med  Hopper-med
    DTQL     57.9    99.6
    VBD      62.8    70.7

被 get_text() 提取后变成（列优先）：

    Method
    HC-med
    Hopper-med
    DTQL
    VBD
    57.9
    62.8
    99.6
    70.7

"DTQL"、"57.9"、"99.6" 在文本里完全分散，对应关系全丢了。LLM 检索到这些散文字，
根本分不清 99.6 是 DTQL 还是 VBD 的——这就是张冠李戴的根源。

### 第 2 层：按行重建后，行标签的 x 坐标又和数字列重叠

改用 `get_text("words")` 拿每个词的坐标，按 y 聚类成"行"后，行结构恢复了，
但又冒出第二个问题：表格的"行标签"（环境名/方法名）在 PDF 里的 x 坐标和数字列重叠，
按 x 排序时标签跑到了行中间。比如 Table 1 的一行变成了：

    88.5±2.2 96.3±0.4 98.6±0.5 Walker2d-med-replay 95.5 73.9 ...

"Walker2d-med-replay" 这个行标签本该在行首，却插到了数字中间，列对齐还是乱的。

### 第 3 层（未完全解决，属 LLM 生成层）：数字密集导致相邻混淆

前两层修好后，还剩一种：表格里上下相邻的两行数字挨得太近（如 VBD 的 62.8 和 VBD-AA 的 77.1），
LLM 读到同一块时仍可能把相邻行的数字安错。这已不是检索问题，而是 LLM 阅读理解的天花板，
属于生成层，靠优化检索无法根治。

## 三、解决方案（两层补丁）

### 补丁 1：按视觉坐标"行优先"提取

用 `get_text("words")` 拿词+坐标，按 y 坐标聚类成"视觉行"（同一行的词 y 相近），行内按 x 从左到右：

```python
def extract_lines(page, y_tol=3.0):
    """按视觉坐标提取一页的'行'，表格行做'标签前置'保持行列对应"""
    import re
    def is_data(w):
        # 数据词：数字、±、—、✓ 等开头（表格单元格内容）
        return bool(re.match(r'^[\d±—✓\-\.\(\)]', w))

    words = page.get_text("words")
    words.sort(key=lambda w: (round(w[1], 1), w[0]))   # 先按 y 行，再按 x 列
    raw_lines = []
    cur, cur_y = [], None
    for w in words:
        y = w[1]
        if cur_y is None or abs(y - cur_y) > y_tol:     # y 差超过阈值 = 新行
            if cur:
                raw_lines.append(cur)
            cur, cur_y = [w[4]], y
        else:
            cur.append(w[4])
    if cur:
        raw_lines.append(cur)

    result = []
    for ws in raw_lines:
        data_count = sum(1 for w in ws if is_data(w))
        label_count = len(ws) - data_count
        if data_count >= 2 and label_count >= 1:        # 表格行特征：多个数据词 + 有标签词
            labels = [w for w in ws if not is_data(w)]
            datas = [w for w in ws if is_data(w)]
            result.append(" ".join(labels + datas))     # 标签前置，数字后置
        else:
            result.append(" ".join(ws))                 # 正文行保持原样
    return result
```

关键点：
- `y_tol=3.0`：y 聚类容差，要能包容 β、✓ 这类特殊字符的坐标偏移（试过 2.0 会拆散，3.0 正好）。
- "标签前置"：判断一行是表格行（数据词≥2 且有标签词）后，把文字标签挪到行首、数字放后面，
  这样行标签和数字列的对应关系就恢复了。

### 补丁 2：按"行"切块，绝不拆散单行

之前按 400 字硬切，可能一刀切在表格行中间。改成按行累积，只在行与行之间切：

```python
def split_chunks(lines, chunk_size=400):
    """按行切块：累积到接近 chunk_size 字就切开，但绝不拆散单行"""
    chunks, current, current_len = [], [], 0
    for line in lines:
        if current and current_len + len(line) > chunk_size:
            chunks.append("\n".join(current))
            current, current_len = [], 0
        current.append(line)
        current_len += len(line)
    if current:
        chunks.append("\n".join(current))
    return chunks
```

## 四、效果验证（前后对比）

| 表格题 | 优化前 | 优化后 | 正确答案 |
|---|---|---|---|
| DTQL 在 Hopper 分数 | 70.7（错） | 99.6 ✅ | 99.6 |
| VBD-AA 在 W2d-replay 分数 | 96.3（错） | 98.6 ✅ | 98.6 |
| VBD-AA 在 HC-med 分数 | 没答出 | 62.8（仍错） | 77.1 |

整体命中率：63%（19/30）→ 70%（21/30）。3 个表格题修好 2 个，第 3 个从"没答出"进步到"答了相邻值"。

## 五、可复用的经验教训

1. **PDF 表格提取必须"行优先"，不能用默认 get_text()**。默认是列优先，会把行列对应关系打散，
   这是 RAG 在 PDF 表格上出错的头号原因，且很难察觉（只有用评估集量化才能发现）。

2. **行标签要"标签前置"**：按行重建后，行标签（文字）可能因为 x 坐标和数字列重叠跑到行中间，
   把"文字放前、数字放后"能恢复列对齐。判断表格行的特征是"数据词≥2 且含标签词"。

3. **评估集是发现这类问题的唯一手段**。没有"问题+标准答案"的量化评估，你根本不会知道
   自己的 RAG 在表格上翻车，只会笼统觉得"好像还行"。评估的价值 = 暴露问题 + 指导优化方向。

4. **切块绝不拆散单行**：固定字数硬切会切断表格行，改成按行累积切块。

5. **分清"检索问题"和"生成问题"**：表格结构打乱是检索问题（能修）；但表格里相邻数字
   被 LLM 混淆是生成层的阅读理解天花板（修不动，只能靠减小 chunk 或加提示缓解）。
   别把生成层的问题误当检索问题去死磕。

## 六、遗留问题 + 后续方向

- 遗留：表格内"相邻行列数字混淆"（LLM 生成层问题），可尝试：减小 chunk_size、给表格加专门提示、
  或对数字类问题加强 BM25 权重。
- 后续方向：更彻底的表格解析可用 `pymupdf_layout` 包，或把 PDF 表格单独转结构化数据（如 Markdown 表格）再入库。
